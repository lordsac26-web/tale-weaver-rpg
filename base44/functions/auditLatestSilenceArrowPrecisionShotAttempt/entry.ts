import { createClientFromRequest } from 'npm:@base44/sdk@0.8.44';
import { characterBelongsToUser } from '../../shared/combat/authGuard.ts';
import { auditSilenceArrowPrecisionShotAttempt } from '../../shared/repairs/silenceArrowPrecisionShotAudit.ts';
export default async function(req){try{const base44=createClientFromRequest(req),user=await base44.auth.me();if(!user)return Response.json({error:'Unauthorized'},{status:401});const payload=await req.json();if(payload?.apply)return Response.json({error:'This forensic endpoint is read-only.'},{status:405});const[character,session]=await Promise.all([base44.asServiceRole.entities.Character.get(payload.character_id),base44.asServiceRole.entities.GameSession.get(payload.session_id)]);if(!character||!session)return Response.json({error:'Character or session not found.'},{status:404});if(!characterBelongsToUser(character,user)||session.character_id!==character.id)return Response.json({error:'Ownership chain is invalid.'},{status:403});return Response.json({...(await auditSilenceArrowPrecisionShotAttempt({db:base44.asServiceRole,character,session})),function_version:'audit-silence-arrow-v1.1.0'});}catch(error){return Response.json({error:error.message||'Silence composite audit failed.'},{status:500});}}